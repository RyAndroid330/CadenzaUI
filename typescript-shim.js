// Empty shim — the `typescript` package is used by @cadenza.io/service for server-side
// code generation and is never needed in the browser. This shim satisfies the import
// without pulling the full typescript compiler into the client bundle.
export default {};
